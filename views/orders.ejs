<%- include('partials/header') %>
<main>
  <h1>Your Orders</h1>
  <ul>
    <% orders.forEach(order => { %>
      <li><%= order.filename %> - $<%= order.price %></li>
    <% }) %>
  </ul>
</main>
<%- include('partials/footer') %>